# School Tracker

An assignment tracker for students

## Run it
1. Install **Node.js (LTS)**.
2. In this folder:

   ```bash
   npm install
   npm start
   ```

3. Open in a browser:

   - http://localhost:3000
